#include <stdio.h>


void Sort(int *a,int low,int high)
{
	int privot = a[low];
	if(low < high)
	{
		while(a[high] >= privot && low < high)
	}
}
void InsertSort(int a[],int length)
{
	int i,j;
	int min_tmp;
	for( i = 0; i < length; i++)
	{
		j = i+1
		if(a[j] < a[i])
		{
			min_tmp = a[i];
			a[j] = a[i];
			a[i] = min_tmp;
		}
	
	
	}


}
